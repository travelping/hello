% vim: filetype=erlang
{application, hello2,
  [{description, "JSON-RPC API toolkit"},
   {applications, [kernel, stdlib, cowboy, ex_uri, erlzmq, ibrowse]},
   {registered, [hello2_supervisor, hello2_stateless_zmq_supervisor]},
   {mod, {hello2, []}},
   {env, [{request_log_dir, "request-logs"}]}
]}.
