% vim: filetype=erlang
{application, hello,
  [{description, "JSON-RPC API toolkit"},
   {vsn, git},
   {applications, [kernel, stdlib, cowboy, ex_uri, ibrowse]},
   {registered, [hello_supervisor]},
   {mod, {hello, []}},
   {env, [{request_log_dir, "request-logs"}]}
]}.
