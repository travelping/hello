% vim: filetype=erlang
{application, hello,
  [{description, "JSON-RPC API toolkit"},
   {applications, [kernel, stdlib, inets, cowboy, ex_uri]},
   {registered, [hello_supervisor, hello_stateless_zmq_supervisor]},
   {mod, {hello, []}},
   {env, [{request_log_enabled, true},
          {request_log_dir, "request-logs"}]}
]}.
